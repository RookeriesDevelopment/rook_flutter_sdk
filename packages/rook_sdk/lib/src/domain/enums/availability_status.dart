/// This enum represents the current status of the health provider application.
///
/// * [installed] - Health provider is installed.
/// * [notInstalled] - Health provider is not installed.
/// * [notSupported] - Health provider is not supported in this device.
enum AvailabilityStatus {
  installed,
  notInstalled,
  notSupported;
}
